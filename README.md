# LSR-XML-Helper
XML editor for LSR configs. Auto-detects XML files, loads them into readable lists, lets you browse by type, edit values instantly, clone entries, and view without changes. Auto-backup on save. Built-in search shows matching files, entries, and fields, with direct jump to editing.
